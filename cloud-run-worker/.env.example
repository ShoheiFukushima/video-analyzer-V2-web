# =====================================
# Cloud Run Worker Environment Variables
# =====================================

# ---------------------------------------
# Supabase Configuration (Dual Mode)
# ---------------------------------------
# USE_SUPABASE controls the status management backend:
# - false: In-memory status (development mode, no Supabase required)
# - true: Supabase status (production mode, requires credentials below)

USE_SUPABASE=false

# Supabase credentials (required only when USE_SUPABASE=true)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# ---------------------------------------
# Vercel Blob Storage
# ---------------------------------------
BLOB_READ_WRITE_TOKEN=vercel_blob_rw_...

# ---------------------------------------
# AI APIs
# ---------------------------------------
# OpenAI (for Whisper transcription)
OPENAI_API_KEY=sk-...your-openai-api-key...

# Gemini Vision API (for OCR)
GEMINI_API_KEY=your-google-gemini-api-key
GEMINI_MODEL=gemini-2.5-flash

# ---------------------------------------
# Worker Security
# ---------------------------------------
WORKER_SECRET=your-secret-key-here

# ---------------------------------------
# Server Configuration
# ---------------------------------------
NODE_ENV=development
PORT=8080

# ---------------------------------------
# Optional Features
# ---------------------------------------
# Google Cloud Storage (Optional, for backup)
GCS_BUCKET=video-analyzer-results

# Logging
LOG_LEVEL=info

# ---------------------------------------
# Usage Examples
# ---------------------------------------
# Development (local):
#   USE_SUPABASE=false
#   NODE_ENV=development
#   - Status stored in memory (temporary)
#   - Results saved to /tmp
#   - No Supabase connection required
#
# Production (Cloud Run):
#   USE_SUPABASE=true
#   NODE_ENV=production
#   - Status stored in Supabase (persistent)
#   - Results saved to Vercel Blob
#   - Supabase credentials required
